# herdr configuration
# https://herdr.dev

[keys]
# Prefix key to enter prefix mode (default: "ctrl+b")
prefix = "ctrl+s"
# Open settings with prefix+,. Double-prefix toggles focused-pane zoom.
settings = "prefix+comma"
zoom = "prefix+ctrl+s"
# Default is prefix+v; use | like tmux's vertical split.
split_vertical = "prefix+|"
# Direct pane navigation, no prefix required.
focus_pane_left = "ctrl+h"
focus_pane_down = "ctrl+j"
focus_pane_up = "ctrl+k"
focus_pane_right = "ctrl+l"
# Cycle through agents in the order shown in the Agent panel.
previous_agent = "cmd+shift+["
next_agent = "cmd+shift+]"

[theme]
name = "tokyo-night"
auto_switch = false
